#### Steps to bring up application in local
1. Setup docker on your desktop
2. Open a powershell from root project directory
3. Run the following command: docker compose up -d
4. This will initialize containers for kafka, schema-registry & oracle db
